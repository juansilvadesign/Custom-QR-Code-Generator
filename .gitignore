
saved
build

# Release fixtures are deterministic derived output; regenerate them with
# `python export_release_fixtures.py` instead of committing ~600 KB of SVG.
fixtures/release
.env
.venv
__pycache__
*.py[cod]
.coverage
.pytest_cache
backup.js
